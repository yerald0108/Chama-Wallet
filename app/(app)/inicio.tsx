// app/(app)/inicio.tsx
import { Screen, AppText } from '@/components/ui'

export default function Inicio() {
  return (
    <Screen>
      <AppText variant="heading">Inicio</AppText>
    </Screen>
  )
}