// app/(app)/enviar/monto.tsx
import { Screen, AppText } from '@/components/ui'

export default function EnviarMonto() {
  return (
    <Screen>
      <AppText variant="heading">Enviar — Monto</AppText>
    </Screen>
  )
}