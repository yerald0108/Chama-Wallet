// app/(app)/enviar/index.tsx
import { Screen, AppText } from '@/components/ui'

export default function EnviarDestinatario() {
  return (
    <Screen>
      <AppText variant="heading">Enviar — Destinatario</AppText>
    </Screen>
  )
}