// app/(app)/enviar/confirmar.tsx
import { Screen, AppText } from '@/components/ui'

export default function EnviarConfirmar() {
  return (
    <Screen>
      <AppText variant="heading">Enviar — Confirmar</AppText>
    </Screen>
  )
}