// app/(app)/recibir.tsx
import { Screen, AppText } from '@/components/ui'

export default function Recibir() {
  return (
    <Screen>
      <AppText variant="heading">Recibir</AppText>
    </Screen>
  )
}